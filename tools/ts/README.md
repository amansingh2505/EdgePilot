# TypeScript tool README

Place TypeScript/Node-based tools here. Each tool should export a module that matches the Tool interface defined in src/runtime/types.ts.

Example entry point: plugins/ts-example/index.ts
